#!/bin/bash
# Clear clipboard first
printf '' | xclip -sel primary
printf '' | xclip -sel secondary
printf '' | xclip -sel clipboard
pkill -f xclip

if [ "$1" == "otp" ]; then
  shift 1
  passmenu-otp $@
else
  passmenu $@
fi
